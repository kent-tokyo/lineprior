[package]
name = "lineprior-adapters"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Typed domain adapters that convert Sekirei, UI, agent, and retrosynthesis records to lineprior observations."

[dependencies]
lineprior = { path = "../lineprior", version = "0.11.1" }
